release=10.1
# Linux/Mac/UNIX
cc.installer=cc-def-${release}-fix5-${os.platform}.sh
repo.product=products-${release}
repo.fix=fixes-${release}

environment.type=server

# MUST update these to match your test host
os.platform=osx
os.credentials=REMOTE
spm.host=sergeis-macbook-pro.local
install.dir=/Users/sergeip/sag/${spm.alias}

spm.alias=test1
spm.port=8292

# is.license.key.alias=
# um.license.key.alias=
# is.memory.init=128
# is.memory.max=256
# um.memory.init=512
# um.memory.max=512
